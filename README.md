# X3

- **recorder/** — X3 Recorder: запись экрана + звук на Java (оверлей-точка с временем,
  пауза/стоп, режим звонка). Сборка: Android Studio/Gradle, CI (aapt) или `recorder/build.sh`.
  Подробности в `recorder/README.md`.
