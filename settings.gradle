pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'X3'

// Модуль приложения — папка recorder/ (общая с aapt-сборкой build.sh и CI)
include ':app'
project(':app').projectDir = file('recorder')
